=content_for :title do
  Planned Trips Dashboard
  
= column_chart  @trips.send("group_by_#{@grouping}", :trip_time).count, 
                title: "Trips Planned by #{@grouping.titleize}"

= link_to "Back", admin_reports_path, method: :get, class: 'btn btn-primary'
