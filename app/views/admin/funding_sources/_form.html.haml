= simple_form_for funding_source,
  url: form_url,
  authenticity_token: true,
  data: {type: 'html'},
  html: {class: 'flex-outer', id: 'form_id_from_path', multipart: true, method: mode == "new" ? :post : :put} do |form|

  .form-group
    =form.input :agency, collection: [[funding_source.agency.name, funding_source.agency_id]], include_blank: false, disabled: true, wrapper: :travel_patterns_input_group
    =form.hidden_field :agency_id
    =form.input :name, placeholder: 'Enter name of funding source.', disabled: mode == 'view', wrapper: :travel_patterns_input_group
  .form-group
    =form.input :description, placeholder: 'Enter description of funding source.', disabled: mode == 'view', wrapper: :travel_patterns_input_group

  .text-center
    - if mode == 'view'
      =link_to 'Done', admin_funding_sources_path, class: 'btn btn-danger'
    -else
      =link_to 'Cancel', admin_funding_sources_path, class: 'btn btn-danger'
      =form.submit class: 'btn btn-primary'