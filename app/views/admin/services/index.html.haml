= render :partial => 'shared/flash'

%div.row
  %div.col-md-3
  %div.col-md-6
    -@services.each do |service|
      .panel.panel-default
        .panel-heading
          %h4
            =service.id
        .panel-body
          ='Replace this whole thing with some version of the service data form from old 1-click'
        .panel-footer
          %center
            -# =button_to 'Delete', admin_eligibility_path(eligibility), {method: :delete, class: "btn btn-danger btn-sm", data: {confirm: "Are you sure?  This action cannot be undone!"}}

%div.row
  %div.col-md-3
  %div.col-md-6
    .panel.panel-primary
      .panel-heading
        %h4
          ="Create a new Service"
      -# = simple_form_for @new_eligibility, url: admin_eligibilities_path, html: {class: 'form-horizontal', multipart: true, method: :post }, authenticity_token: true do |f|
      -#   .panel-body.panel-primary
      -#     %div.col-md-12
      -#       %label
      -#         = "Code"
      -#       = f.input :code, required:true, label: false, :as => :string, input_html: {style: "width:100%;"}
      -#   .panel-footer.panel-primary
      -#     %center
      -#       = f.submit "OK", class: 'btn action-button btn-primary', role: 'button'
